<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Form view dari purchase order-->
    <record id="purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.purchase.discount.total</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <!-- <xpath expr="//field[@name='state']" position="before">
                <button string="Setujui" type="object" name="action_approve"
                        invisible="state != 'waiting'" class="oe_highlight"
                        groups="purchase.group_purchase_manager"/>
            </xpath> -->
            <!-- <xpath expr="//page[@name='purchase_delivery_invoice']/group" position="before">
                <group col="6">
                    <group colspan="2" name="discount">
                        <field name="discount_type"/>
                        <field name="discount_rate"/>
                    </group>
                    <group class="oe_subtotal_footer oe_right" colspan="2" name="purchase_total">
                        <field name="amount_untaxed" widget='monetary' options="{'currency_field': 'currency_id'}"/>
                        <field name="amount_discount" widget='monetary' options="{'currency_field': 'currency_id'}"/>
                        <field name="amount_tax" widget='monetary' options="{'currency_field': 'currency_id'}"/>
                        <div class="oe_subtotal_footer_separator oe_inline o_td_label">
                            <label for="amount_total"/>
                            <button name="button_dummy" 
                                    invisible="state not in ['draft', 'sent']" 
                                    string="(perbarui)" 
                                    type="object" 
                                    class="oe_edit_only oe_link"/>
                        </div>
                        <field name="amount_total" nolabel="1" class="oe_subtotal_footer_separator" widget='monetary' options="{'currency_field': 'currency_id'}"/>
                    </group>
                </group>
            </xpath> -->
            <!-- Mengubah format field diskon -->
            <xpath expr="//field[@name='order_line']/tree//field[@name='discount']" position="attributes">
                <attribute name="digits">[16, 2]</attribute>
            </xpath>
            <!-- Menambahkan field total_discount setelah price_subtotal --> -->
            <!-- <xpath expr="//field[@name='order_line']/tree//field[@name='price_subtotal']" position="after">
                <field name="total_discount" invisible="1" force_save="1"/>
            </xpath> -->
        </field>
    </record>
</odoo> 